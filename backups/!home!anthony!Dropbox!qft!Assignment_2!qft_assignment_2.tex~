% author lists, or if there are many overlapping affiliations.
% For Phys. Rev. appearance, change preprint to twocolumn.
% Choose pra, prb, prc, prd, pre, prl, prstab, prstper, or rmp for journal
%  Add 'draft' option to mark overfull boxes with black boxes
%  Add 'showpacs' option to make PACS codes appear
%  Add 'showkeys' option to make keywords appear

\documentclass[aps,prl,preprint]{revtex4-1}

\usepackage{amsmath,amssymb}
\usepackage{simplewick}
\usepackage{physics}
\usepackage{slashed}
\usepackage{mypackage}
\usepackage{blindtext}
\usepackage{listings}
\usepackage{color}
\usepackage{graphicx}
\usepackage{float}
\usepackage{hyperref}
\usepackage{tikz}

\usetikzlibrary{calc,decorations.markings}


\newcommand{\eipx}{e^{ip\cdot x}}
\newcommand{\enipx}{e^{-ip\cdot x}}
\newcommand{\eipy}{e^{ip\cdot y}}
\newcommand{\enipy}{e^{-ip\cdot y}}

\begin{document}

% Use the \preprint command to place your local institutional report
% number in the upper righthand corner of the title page in preprint mode.
% Multiple \preprint commands are allowed.
% Use the 'preprintnumbers' class option to override journal defaults
% to display numbers if necessary
% \preprint{}

\title{QFT Assignment 2}

% repeat the \author .. \affiliation  etc. as needed
% \email, \thanks, \homepage, \altaffiliation all apply to the current
% author. Explanatory text should go in the []'s, actual e-mail
% address or url should go in the {}'s for \email and \homepage.
% Please use the appropriate macro foreach each type of information

% \affiliation command applies to all authors since the last
% \affiliation command. The \affiliation command should follow the
% other information
% \affiliation can be followed by \email, \homepage, \thanks as well.

\author{Anthony Kalaitzis}

%\email[]{Your e-mail address}
%\homepage[]{Your web page}
%\thanks{}
%\altaffiliation{}


%Collaboration name if desired (requires use of superscriptaddress
%option in \documentclass). \noaffiliation is required (may also be
%used with the \author command).
%\collaboration can be followed by \email, \homepage, \thanks as well.
%\collaboration{}
%\noaffiliation

\date{\today}
\maketitle

\section{Question 1a}
First, consider the fact that $b_{\vb{p}}\ket{0}=c_{\vb{p}}\ket{0}=0$ and $\bra{0}c^\dg_{\vb{p}}=\bra{0}b^\dg_{\vb{p}}=0$. Hence in the vev we expect only factors that go like $\mel{0}{bb^\dg}{0}$,$\mel{0}{bc^\dg}{0}$,$\mel{0}{cb^\dg}{0}$ and $\mel{0}{cc^\dg}{0}$ to survive.
\begin{align*}
  \bra{0}(2\pi)^6\phi(x)\phi(y)^\dg\ket{0}&=\bra{0}\triint{p}\triint{p'}\frac{1}{\sqrt{2\omega_{\vb{p}}2\omega_{\vb{p'}}}}\qty(b_{\vb{p}}\enipx+c_{\vb{p}}^\dg\eipx)\qty(b^\dg_{\vb{p'}}e^{ip'\cdot y}+c_{\vb{p'}}e^{-ip'\cdot y})\ket{0}\\
  &=\bra{0}\triint{p}\triint{p'}\frac{1}{\sqrt{2\omega_{\vb{p}}2\omega_{\vb{p'}}}}b_{\vb{p}}b_{\vb{p'}}^\dg\enipx e^{ip'\cdot y}\ket{0}
\end{align*}
Using that $\comm{b_{\vb{p}}}{b^\dg_{\vb{p'}}}=(2\pi)^3\delta^3({\vb{p}}-{\vb{p'}})$ gives us that
\begin{align*}
  \text{LHS}&=\mel{0}{\triint{p}\triint{p'}\frac{1}{\sqrt{2\omega_{\vb{p}}2\omega_{\vb{p'}}}}\qty( (2\pi)^3\delta^3({\vb{p}}-{\vb{p'}})+b^\dg_{\vb{p'}}b_{\vb{p}})\enipx e^{ip'\cdot y}}{0}\\
  \implies\mel{0}{\phi(x)\phi^\dg(y)}{0}&=\triint{p}\frac{1}{(2\pi)^32\omega_{\vb{p}}} \ e^{-ip\cdot(x-y)}\ip{0}{0}\\
  \implies \mel{0}{\phi(x)\phi^\dg(y)}{0}&=\triint{p}\frac{1}{(2\pi)^32\omega_{\vb{p}}}e^{-ip\cdot(x-y)}\\
  &=D(x-y)
\end{align*}
\begin{align*}
  \bra{0}(2\pi)^6\phi^\dg(y)\phi(x)\ket{0}&=\mel{0}{\triint{p}\triint{p'}\frac{1}{\sqrt{2\omega_{\vb{p}}2\omega_{\vb{p'}}}}\qty(b^\dg_{\vb{p'}}e^{ip'\cdot y}+c_{\vb{p'}}e^{-ip'\cdot y})\qty(b_{\vb{p}}\enipx+c_{\vb{p}}^\dg\eipx)}{0}\\
                                          &=\triint{p}\triint{p'}\frac{1}{\sqrt{2\omega_{\vb{p}}2\omega_{\vb{p'}}}}e^{-ip'\cdot y}e^{ip\cdot x}\mel{0}{c_{\vb{p'}}c_{\vb{p}}^\dg }{0}\\
                                          &=\triint{p}\triint{p'}\frac{1}{\sqrt{2\omega_{\vb{p}}2\omega_{\vb{p'}}}}e^{-ip'\cdot y}e^{ip\cdot x}\mel{0}{\qty((2\pi)^3\delta(\vb{p'}-\vb{p})-c_{\vb{p}}^\dg c_{\vb{p'}})}{0}\\
  \implies \mel{0}{\phi^\dg(y)\phi(x)}{0}  &=\triint{p}\frac{1}{(2\pi)^32\omega_{\vb{p}}}e^{-ip\cdot(y-x)}\ip{0}{0}\\
                                          &=D(y-x)
\end{align*}

\section{Question 1B}
Consider the following manipulation
\begin{align*}
  p^2-m^2&=p^\mu g_{\mu\nu}p^\nu-m^2=p_0^2-\abs{\vb{p}}^2-m^2=p_0^2-\omega_{\vb{p}}^2
\end{align*}
and
\begin{align*}
  (p_0-\omega_{\vb{p}}+i\epsilon)(p_0+\omega_{\vb{p}}-i\epsilon)&=p_0^2-\omega_{\vb{p}}^2+2i\epsilon\omega_{\vb{p}}+O\qty(\epsilon^2)\\
                                                                &=p_0^2-\omega_{\vb{p}}^2+i\epsilon
\end{align*}
as $\epsilon$ is an infinitesimal hence and where we used that $p^\mu=(\omega_{\vb{p}},\vb{p})$ and $\omega_{\vb{p}}^2=\abs{\vb{p}}^2+m^2$. then we have that
\begin{align*}
  \mel{0}{T\qty[\phi(x)\phi^\dg(y)]}{0}&=\int\frac{\dd^4 p}{(2\pi)^4}\frac{i}{p^2-m^2+i\epsilon}e^{-ip\cdot(x-y)}\\
                                       &=\triint{p} \ e^{i\vb{p}\cdot(\vb{x}-\vb{y})}\int\frac{\dd p_0}{(2\pi)^4}\frac{i}{p^2_0-\omega^2_{\vb{p}}+i\epsilon}e^{-ip_0(x^0-y^0)}\\
                                       &=\triint{p} \ e^{i\vb{p}\cdot(\vb{x}-\vb{y})}\int\frac{\dd p_0}{(2\pi)^4}\frac{i}{(p_0-\omega_{\vb{p}}+i\epsilon)(p_0+\omega_{\vb{p}}-i\epsilon)}e^{-ip_0(x^0-y^0)}
\end{align*}
Which has poles at $p_0=\pm\omega_{\vb{p}}\mp i\epsilon$. Consider first the region where $x^0>y^0\implies \exp(-ip_0(x^0-y^0))<0$. Consider taking the semi circular contour integral enclosed below the real axis. This contour will look like \\

\begin{center}
\begin{tikzpicture}[decoration={markings,
    mark=at position 1.0cm with {\arrow{latex}},
    mark=at position 10.0cm with {\arrow{latex}}
}
]
% The axes
\draw[help lines,->] (-4,0) -- (4,0) coordinate (xaxis);
\draw[help lines,->] (0,-4) -- (0,4) coordinate (yaxis);

% The path
\path[draw,line width=0.8pt,postaction=decorate] (-3,0) node[above] {$-R$} -- (3,0) node[above] {$R$} arc (0:-180:3);

% The labels
\node[below] at (xaxis) {Im$(p_0)$};
\node[left] at (yaxis) {Re$(p_0)$};
\node at (1.8,-1.8) {$C_{r}$};
\node at (-1.5,0.3) {$\cross$};
\node at (1.5,-0.3) {$\cross$};
\node at (-1.7,-0.3) {$-\omega_{\vb{p}}+i\epsilon$};
\node at (1.5,0.3) {$\omega_{\vb{p}}-i\epsilon$};
\end{tikzpicture}
\end{center}

Where the crosses represent the poles of the contour. In this form it is easy to now use Cauchy's integral formula to evaluate these poles where we have that.

\begin{align*}
  \oint f(z)\dd z=\int_{-R}^Rf(z)\dd z+\int_{C_r}f(z)\dd z
\end{align*}
\begin{align*}
  \abs{\int_{C_r}f(z)\dd z}\leq \int_{C_r}\frac{\abs{ie^{-ip_0(x^0-y^0)}}}{\abs{p^2_0-\omega^2_{\vb{p}}+i\epsilon}}\dd s
\end{align*}
Where this becomes an integral over arc length $\dd s$
\begin{align*}
   \abs{\int_{C_r}f(z)\dd z}&\leq\frac{1}{R^2+\epsilon^2+\abs{\omega_{\vb{p}}}^2}\pi R
\end{align*}
Where we have used that $\abs{z-w}\geq\abs{z}-\abs{w}$, $\abs{z+w}\leq\abs{z}+\abs{w}$ and that $\abs{p_0}\leq R$. hence
\begin{align*}
  \abs{\int_{C_r}f(z)\dd z}\to 0
\end{align*}
as $R\to\infty$. So in the limit as $R\to\infty$ we have that
\begin{align*}
  \oint f(z)\dd z=\int_{-\infty}^\infty f(z)\dd z+0
\end{align*}
However, this is now only an integral over the real numbers. So we have that
\begin{align*}
  \oint f(z)\dd z=\int_{-\infty}^\infty f(z)\dd x
\end{align*}
where $x\inR$. We may now just use Cauchy's integral formula to evaluate this remaining integral.
\begin{align*}
  \int_{-\infty}^\infty\frac{\dd p_0}{(2\pi)^4}\frac{i}{(p_0-\omega_{\vb{p}}+i\epsilon)(p_0+\omega_{\vb{p}}-i\epsilon)}e^{-ip_0(x^0-y^0)}=\\
  \oint\frac{\dd p_0}{(2\pi)^4}\frac{i}{(p_0-\omega_{\vb{p}}+i\epsilon)(p_0+\omega_{\vb{p}}-i\epsilon)}e^{-ip_0(x^0-y^0)}
\end{align*}
and the $\exp$ function is entire so we just evaluate the function at the enclosed poles. Which is $p_0=\omega_{\vb{p}}-i\epsilon$ for the below contour.

\begin{align*}
  \oint\frac{\dd p_0}{(2\pi)^4}\frac{i}{(p_0-\omega_{\vb{p}}+i\epsilon)(p_0+\omega_{\vb{p}}-i\epsilon)}e^{-ip_0(x^0-y^0)}&=-\frac{2\pi i}{(2\pi)^4}\qty[ \frac{ie^{-ip_0(x^0-y^0)}}{p_0+\omega_{\vb{p}}-i\epsilon} ]_{p_0=\omega_{\vb{p}}-i\epsilon}\\
                                                                                                                         &=\frac{1}{(2\pi)^3}\frac{e^{-i(\omega_{\vb{p}}-i\epsilon)(x^0-y^0)}}{2\omega_{\vb{p}}-2i\epsilon}
\end{align*}
Note that we picked up the minus sign from Cauchy's integral formula as we used opposite orientation on the integral to achieve the correct integral from $-R$ to $R$. Now let $\epsilon\to 0$ hence
\begin{align*}
  \text{LHS}=-\frac{1}{(2\pi)^3}\frac{e^{-i\omega_{\vb{p}}(x^0-y^0)}}{2\omega_{\vb{p}}}
\end{align*}
hence
\begin{align*}
  \mel{0}{T\qty[\phi(x)\phi^\dg(y)]}{0}&=\triint{p} \ e^{i\vb{p}\cdot(\vb{x}-\vb{y})}\frac{1}{(2\pi)^3}\frac{e^{-i\omega_{\vb{p}}(x^0-y^0)}}{2\omega_{\vb{p}}}\\
                                       &=\triint{p}\frac{1}{2\omega_{\vb{p}}(2\pi)^3}e^{-ip\cdot(x-y)}\\
                                       &=D(x-y)
\end{align*}
for $x^0>y^0$. For $x^0<y^0$ we have that $\exp(-ip_0(x^0-y^0))>$ so we close the contour like so.

\begin{center}
\begin{tikzpicture}[decoration={markings,
    mark=at position 1.0cm with {\arrow{latex}},
    mark=at position 10.0cm with {\arrow{latex}}
}
]
% The axes
\draw[help lines,->] (-4,0) -- (4,0) coordinate (xaxis);
\draw[help lines,->] (0,-4) -- (0,4) coordinate (yaxis);

% The path
\path[draw,line width=0.8pt,postaction=decorate] (-3,0) node[below] {$-R$} -- (3,0) node[below] {$R$} arc (0:180:3);

% The labels
\node[below] at (xaxis) {Im$(p_0)$};
\node[left] at (yaxis) {Re$(p_0)$};
\node at (1.8,1.8) {$C_{r}$};
\node at (-1.5,0.3) {$\cross$};
\node at (1.5,-0.3) {$\cross$};
\node at (-1.7,-0.3) {$-\omega_{\vb{p}}+i\epsilon$};
\node at (1.5,0.3) {$\omega_{\vb{p}}-i\epsilon$};
\end{tikzpicture}
\end{center}

Following the exact same argument as before, we can just apply Cauchy's integral formula to evaluate this integral. This time however we are using standard orientation so no extra minus sign will appear. Hence for $x^0<y^0$ we have that

\begin{align*}
  \oint\frac{\dd p_0}{(2\pi)^4}\frac{i}{(p_0-\omega_{\vb{p}}+i\epsilon)(p_0+\omega_{\vb{p}}-i\epsilon)}e^{-ip_0(x^0-y^0)}&=\frac{2\pi i}{(2\pi)^4}\qty[ \frac{ie^{-ip_0(x^0-y^0)}}{p_0-\omega_{\vb{p}}+i\epsilon} ]_{p_0=-\omega_{\vb{p}}+i\epsilon}\\
                                                                                                                         &=-\frac{1}{(2\pi)^3}\frac{e^{-i(-\omega_{\vb{p}}+i\epsilon)(x^0-y^0)}}{-2\omega_{\vb{p}}+2i\epsilon}
\end{align*}

Now let $\epsilon\to 0$ hence
\begin{align*}
  \text{LHS}=\frac{1}{(2\pi)^3}\frac{e^{-i\omega_{\vb{p}}(y^0-x^0)}}{2\omega_{\vb{p}}}
\end{align*}
hence
\begin{align*}
  \mel{0}{T\qty[\phi(x)\phi^\dg(y)]}{0}&=\triint{p} \ e^{i\vb{p}\cdot(\vb{x}-\vb{y})}\frac{1}{(2\pi)^3}\frac{e^{-i\omega_{\vb{p}}(y^0-x^0)}}{2\omega_{\vb{p}}}\\
                                       &=\triint{p}\frac{1}{(2\pi)^32\omega_{\vb{p}}}e^{-i\omega_{\vb{p}}(y^0-x^0)-i\vb{p}\cdot(\vb{y}-\vb{x})}
\end{align*}
Note the symmetric integral from $-\infty$ to $\infty$ so make the transformation that $\vb{p}\to\vb{-p}$ hence
\begin{align*}
  \mel{0}{T\qty[\phi(x)\phi^\dg(y)]}{0}&=\triint{p}\frac{1}{(2\pi)^32\omega_{\vb{p}}}e^{-i\omega_{\vb{p}}(y^0-x^0)+i\vb{p}\cdot(\vb{y}-\vb{x})}\\
                                       &=\triint{p}\frac{1}{(2\pi)^32\omega_{\vb{p}}}e^{-ip\cdot(y-x)}\\
                                       &=D(y-x)
\end{align*}
Hence, this gives us our appropriate propagators from Question 1a under time ordered conditions i.e. we have shown that
\begin{align*}
  \mel{0}{T\qty[\phi(x)\phi^\dg(y)]}{0}=
  \begin{cases}
    \ D(x-y) & \qquad x^0>y^0\\
    \ D(y-x) & \qquad y^0>x^0
  \end{cases}\
\end{align*}

\section{Question 2a}
Wicks Theorem for the time ordering of normal ordered products says that we take the normal ordering of all possible contractions of the operators that aren't in the same normal ordering like so ($\phi_i\equiv\phi(x_i)$).






















% only one appendix.
%\appendix
%\section{}

%\begin{acknowledgments}
% put your acknowledgments here.
%\end{acknowledgments}

% Create the reference section using BibTeX:
\bibliography{basename of .bib file}

\end{document}

% \bibliography{basename of .bib file}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:

%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
