program test
  use kinds
  use numFort
  use pyplots
  use LAPACK95
  implicit none

  integer,parameter::N=500
  real(DP),dimension(N)::x,y,z,w

  x = linspace(-5.0,5.0,N)
  y = sin(x)
  z = x
  w = cos(z)

  call plot(x,y)










end program test
