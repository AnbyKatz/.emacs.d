# Fortran base MakeFile
# add links to libaries if needed

F90C		= ifort
F90FLAGS	= -O2
CFLAGS		= -O2 -mkl
LIBS		= -lmkl_lapack95_lp64
MYLIBS          = -I/home/anthony/Dropbox/Code/Fortran/f90-toolbox/
PATH            = /home/anthony/Dropbox/Code/Fortran/f90-toolbox/

# %.o: %.f90
# 	$(F90C) $(CFLAGS) -c $<

# Modules below
LIBOBJS = kinds.o\
	pyplots.o\
	lapack.o\
	numFort.o\
	quadpack.o

all: test

test: test.f90 
	$(F90C) $(CFLAGS) -o $@ $< $(LIBOBJS) $(LIBS) $(MYLIBS)

clean:
	rm *.o *.mod
