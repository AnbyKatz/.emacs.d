program plot_test
  use kinds
  use UsefulFunctions
  use pyplots
  implicit none

  integer , parameter    :: N = 10**3
  real(dp), dimension(N) :: x,y,z,w
  
  x = linspace(-5,5,N)
  y = sin(x)
  z = x
  w = cos(z)

  call plotxy(x,y,z,w,"x_axis","y_axis")

  
end program plot_test
